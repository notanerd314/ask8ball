import { useState } from 'react';

export const OptionsButtons: React.FC = () => {

    

    return (
        <div id="optionsButtons">
            <button class="bottom_button" onClick={}>Options</button>
        </div>
    );
};